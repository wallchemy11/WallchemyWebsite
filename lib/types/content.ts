export type SelectedWorkItem = {
  title: string;
  slug: string;
  heroImage: string;
  description?: string;
};

export type MaterialLibraryItem = {
  title: string;
  slug: string;
  heroImage: string;
  description?: string;
};
